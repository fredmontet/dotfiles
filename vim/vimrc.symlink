" -------------------------------------------------------------
" .vimrc
"
" Author: Fred Montet
" Inspired from: http://dougblack.io/words/a-good-vimrc.html
" -------------------------------------------------------------

" UI Config
" -------------------------------------------------------------

set showmatch           " highlight matching [{()}]
set confirm				" better dialog
set mouse=a 			" enable mouse for all modes
set pastetoggle=<F11>	" F11 to toggle paste mode
set wrap				" set word wrap
set wm=1

" Movement
" -------------------------------------------------------------

" move vertically by visual line
"nnoremap j gj
"nnoremap k gk

" move to beginning/end of line
"nnoremap B ^
"nnoremap E $

" $/^ doesn't do anything
"nnoremap $ <nop>
"nnoremap ^ <nop>

" highlight last inserted text
"nnoremap gV `[v`]


" Leader shortcuts
" -------------------------------------------------------------

let mapleader=","       " leader is comma
"inoremap jk <esc>		" jk is escape


" Base from Zack Holman dotfiles
" -------------------------------------------------------------

syntax on

" Wrap gitcommit file types at the appropriate length
filetype indent plugin on
